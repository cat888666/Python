# 一，汉语简单释义
1. **print**
实现打印输出功能·。
2. 变量
储存或者指代某个值（与C语言含义类似）
3. 


# 二，语法演示
1. *print*
print（"*随意内容*")  (单引号双引号都可以)
进阶
* 字符串连接 print("你好"+"再见")
* 单引号双引号转换：
不需要打印引号时**内双外单，内单外双** print("我说'你好'")
  打印出来：我说你好
需要打印时,**加上转义字符反斜杠** print("I say Let\\'s go!")
  打印出来：I say Let's go!
  * 换行：\\n
  * 自动换行：
     """东西1
     东西2"""(单双引号都可以，需要连续打三个)

*print("hello!")  
print("hello"+"world"+"!")  
print("I say 'hello!'")  
print("I say \"Let\'go!\"")  
print("第一行\n第二行")  
print("--------------")  
print('''第一行  
第二行''')*
2. *变量*
* 变量起名不能数字打头；不能有空格；不能用引号包裹；
* 变量赋值：变量名="值"(这样在用print打印的时候不需要再给这个变量加引号，用+连接即可)




3. 